import React from "react";
import { Login as LoginVodo } from "vodo-react-components";
interface LoginProps {}

const Login: React.FC<LoginProps> = ({}) => {
  return <LoginVodo />;
};

export default Login;
